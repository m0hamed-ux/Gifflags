config:
  target: "http://localhost:3000"
  phases:
    - duration: 30
      arrivalRate: 1000
scenarios:
  - flow:
      - post:
          url: "/handleIncrease"
          json:
            countryName: "France"